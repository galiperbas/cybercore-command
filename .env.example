# Google Gemini API key (required for the AI chatbot feature)
GEMINI_API_KEY="your_gemini_api_key_here"

# Custom WebSocket URL (optional — defaults to ws://localhost:8000/ws)
# Useful when the backend runs on a different host or port.
# VITE_WS_URL="ws://192.168.1.100:8000/ws"
